{
  "name": "@extraction/extension",
  "version": "0.1.0",
  "private": true,
  "description": "PROJECT EXTRACTION - Browser extension for distraction monitoring (Manifest V3)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "clean": "rm -rf dist .turbo",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
